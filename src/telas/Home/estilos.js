import { StyleSheet } from "react-native";

export default StyleSheet.create({
    preencher: {
        flex:1,
        flexDirection: 'row',
        justifyContent: 'center',
        marginVertical: 10,
    },
    quadroVariavel:{
        textAlign:'center',
        borderColor: '#000',
        justifyContent: 'center',
    }
});
